# React Native / Expo dependency trees have many overlapping peer ranges that
# npm's strict resolver rejects (ERESOLVE). Legacy peer resolution is the
# ecosystem-standard setting and keeps `npm install` / `expo install` reliable.
legacy-peer-deps=true
