# Optional API override. If unset, the app uses a platform default:
#   iOS simulator  -> http://localhost:8000/api/v1
#   Android emulator -> http://10.0.2.2:8000/api/v1
# For physical devices or builds, set this in app.json -> expo.extra.apiBaseUrl.
API_BASE_URL=http://localhost:8000/api/v1
